@page
@model PayrollWebsite.Pages.Payroll.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Payroll[0].StartDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Payroll[0].StopDate)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Payroll[0].Employee)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Payroll) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StopDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.EmployeeId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PayrollId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PayrollId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PayrollId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
